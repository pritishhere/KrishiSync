# MERN Stack Starter

Minimal monorepo scaffold for a MERN app.

## Structure

- `client` - React frontend
- `server` - Express API

## Next Steps

1. Install dependencies in the root workspace.
2. Add environment variables in `server/.env`.
3. Build out routes, models, and React pages.
