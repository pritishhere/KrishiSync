{
  "name": "hackathon-mern",
  "private": true,
  "workspaces": [
    "server",
    "KrishiSync"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace krishisync\"",
    "install:all": "npm install --workspaces",
    "setup": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
